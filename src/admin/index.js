// Do Admin Page Code here... REACT?
import React from 'react';
import { render } from 'react-dom';
import App from '../containers/app';

const rootEl = document.getElementById('admin-app');


render(<App />, rootEl);

